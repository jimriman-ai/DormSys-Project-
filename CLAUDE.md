# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

**DormSys** is a greenfield enterprise Laravel 13application for employee dormitory request, allocation, lottery, and lifecycle management. It follows a **Modular Monolith + Clean Architecture + DDD Lite** pattern. The authoritative governance document is `.specify/memory/constitution.md`.

<!-- SPECKIT START -->
For additional context about technologies to be used, project structure,
shell commands, and other important information, read the current plan
at specs/010-audit-trail/plan.md
<!-- SPECKIT END -->

## Tech Stack

- **Backend:** Laravel 13, PHP 8.4
- **Frontend:** Livewire 3 (server-first), Alpine.js (micro-interactions only), Tailwind CSS (RTL/Persian)
- **Database:** PostgreSQL 17 (UUID primary keys, exclusion constraints for overlap prevention)
- **Cache/Queue:** Redis (Laravel Queue + Horizon)
- **Packages:** `spatie/laravel-model-states`, `spatie/laravel-activitylog`, `spatie/laravel-permission`, `morilog/jalali`
- **Dev Environment:** Docker Sail
- **Testing:** Pest PHP (unit + feature), PHPUnit, optional Laravel Dusk

## Common Commands

```bash
# Start environment
sail up

# Run all tests
sail artisan test

# Run a single test file
sail artisan test tests/Unit/Modules/Lottery/LotteryScoringEngineTest.php

# Run tests matching a filter
sail artisan test --filter=LotteryScoringEngine

# Database migrations
sail artisan migrate

# Static analysis
sail composer run phpstan   # PHPStan level 8

# Code formatting
sail composer run pint      # Laravel Pint

# Queue worker (local)
sail artisan horizon
```

## Module Architecture

All business logic lives under `app/Modules/`. Each module follows a strict 4-layer structure:

```
app/Modules/{Module}/
├── Domain/          # Entities, Value Objects, Domain Services, Domain Events
├── Application/     # Use Cases (Actions/Commands), DTOs, Application Services
├── Infrastructure/  # Eloquent Models (persistence adapters), Repositories, Jobs
└── Presentation/    # Livewire Components, Controllers, Blade Views, Form Requests
```

**Core modules:** `Identity`, `Employee`, `Request`, `Workflow` (deferred orchestration), `Dormitory`, `Allocation`, `Lottery`, `Notification`, `Audit`, `Reporting`, `Voucher`, `CheckIn`

**CheckIn / CheckOut:** Spec07 **CLOSED** — operational stay lives under `app/Modules/CheckIn/` (no separate `CheckOut` module directory). Do not treat as “candidate / inactive.”

**Shared kernel** lives in `app/Support/` — `BaseModel`, `HasUuid`, `RecordsActivity`, value objects, and repository contracts.

## Critical Architecture Rules

1. **Layer dependency direction:** Domain ← Application ← Infrastructure/Presentation. The Domain layer must never import from outer layers.
2. **Eloquent models belong in Infrastructure**, not Domain. Domain entities are pure PHP classes.
3. **No cross-module Eloquent queries.** Cross-module reads go through Application Services. Cross-module foreign keys are prohibited (store UUIDs as value references without FK constraints).
4. **State machines** (`spatie/laravel-model-states`) own all entity lifecycle transitions — never scatter transition logic in Livewire components or controllers.
5. **Audit log is append-only.** Never UPDATE or DELETE from `audit_logs`. All state transitions must emit audit entries via `AuditService`.
6. **Alpine.js** is for dropdown toggles, modals, and micro-interactions only. All primary interactivity goes through Livewire.

## Key Business Rules (Summary)

- **Allocation overlap:** PostgreSQL exclusion constraint on `(person_id, daterange)` prevents double-booking. Zero incidents is the target.
- **Lottery scoring** is deterministic: `weighted_score = normalized_weight + PRNG(random_seed, registration_id)`. The formula is configurable via `settings` table — never hardcode it.
- **Family requests** always receive room-based (private room) allocation; bed-based is prohibited.
- **Approval workflow** is four stages: DeptMgr → HRMgr → DormMgr → DormUnitMgr. Each stage may be configured for auto-approval via `settings` table.
- **External dormitories** generate voucher codes for lottery winners; no physical bed tracking or check-in/out.
- **Check-in/out** is only for internal dormitories and can only be performed by the `Operator` role.

## Speckit Workflow

This project uses **Speckit** for spec-driven development. Features are organized under `specs/{###-feature-name}/`:

```
specs/{###-feature-name}/
├── spec.md       # Feature specification (source of truth for requirements)
├── plan.md       # Implementation plan (generated by /speckit-plan)
└── tasks.md      # Task list (generated by /speckit-tasks)
```

Available slash commands: `/speckit-specify`, `/speckit-plan`, `/speckit-tasks`, `/speckit-implement`, `/speckit-analyze`, `/speckit-clarify`.

The `plan.md` in each feature directory is what `CLAUDE.md` refers to as "the current plan" — read it for technical context before implementing a feature.

## Definition of Done

A task is complete when:
1. PHPStan level 8 passes with no errors
2. Laravel Pint formatting applied
3. Domain/Application layer test coverage >80%
4. All state transitions emit audit log entries via `AuditService`
5. Database migrations include a rollback

## Localization

- UI language: **Persian (Farsi)**, RTL layout
- Dates displayed in **Jalali calendar** using `morilog/jalali`
- All timestamps stored in **UTC** in PostgreSQL; conversion at presentation layer
